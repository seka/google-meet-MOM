import { initializeAboutSection } from "@features/settings/components/about-section/about-section";
import { initializeConnectionTest } from "@features/settings/components/connection-test/connection-test";
import { initializeSettingsForm } from "@features/settings/components/settings-form/settings-form";
import { initializeSettingsNavigation } from "@features/settings/components/settings-navigation/settings-navigation";

initializeSettingsNavigation();
initializeSettingsForm();
initializeConnectionTest();
initializeAboutSection();
