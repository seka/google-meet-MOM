import { initializeAboutSection } from "@features/theme-settings/components/about-section/about-section";
import { initializeConnectionTest } from "@features/theme-settings/components/connection-test/connection-test";
import { initializeSettingsForm } from "@features/theme-settings/components/settings-form/settings-form";
import { initializeSettingsNavigation } from "@features/theme-settings/components/settings-navigation/settings-navigation";

initializeSettingsNavigation();
initializeSettingsForm();
initializeConnectionTest();
initializeAboutSection();
